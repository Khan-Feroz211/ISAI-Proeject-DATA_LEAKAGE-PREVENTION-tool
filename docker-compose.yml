version: '3.8'

services:
  dlp-scanner:
    build: .
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=production
    volumes:
      - dlp-data:/app/data
    restart: unless-stopped

volumes:
  dlp-data:
